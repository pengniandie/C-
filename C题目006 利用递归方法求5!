//利用递归方法求5!
#include<stdio.h>
int main()
{
    int fun (int n);
    for(int i=0; i<=5; i++)
    {
        printf("%d\n",fun(i));
    }
}

int fun (int n)
{
    if(n==1 || n==0)
    {
        return 1;
    }
    else
    {
        return n * fun (n-1);
    }
}
